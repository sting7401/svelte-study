import type { PageLoad } from './$types';

// export const load = (async ({ fetch, params }) => {
// 	const res = await fetch(`db.json`);
// 	const item = await res.json();

// 	return {
// 		post: {
// 			title: `Title for goes ${item.type} here`,
// 			content: `Content for goes here`
// 		}
// 	};
// }) satisfies PageLoad;
