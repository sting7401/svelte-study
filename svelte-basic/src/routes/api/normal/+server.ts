import type { RequestHandler } from './$types';
import { json } from '@sveltejs/kit';

// export const GET: RequestHandler = async ({ fetch }) => {
// 	const res = await fetch('../../../lib/json/notice.json');
// 	const result = await res.json();

// 	return json(result);
// };
